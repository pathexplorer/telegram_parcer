{
  "displayName": "telegramPoller — CRITICAL errors",
  "documentation": {
    "content": "The telegramPoller Gen 2 Cloud Function logged a CRITICAL-level error (logged under resource type cloud_run_revision / service telegrampoller). This means the function has crashed or cannot start (e.g. Secret Manager failure, Firestore failure, import error, or unhandled polling exception). Check Cloud Logging for the full traceback.",
    "mimeType": "text/markdown"
  },
  "combiner": "OR",
  "conditions": [
    {
      "displayName": "Critical log entries from telegramPoller",
      "conditionMatchedLog": {
        "filter": "resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"telegrampoller\" AND severity>=CRITICAL"
      }
    }
  ],
  "alertStrategy": {
    "notificationRateLimit": {
      "period": "300s"
    },
    "autoClose": "604800s"
  },
  "enabled": true
}
