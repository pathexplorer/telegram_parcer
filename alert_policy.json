{
  "displayName": "telegramPoller — CRITICAL errors",
  "documentation": {
    "content": "The telegramPoller Cloud Function logged a CRITICAL-level error. This means the function has crashed or cannot start (e.g. Secret Manager failure, Firestore failure, import error, or unhandled polling exception). Check Cloud Logging for the full traceback.",
    "mimeType": "text/markdown"
  },
  "combiner": "OR",
  "conditions": [
    {
      "displayName": "Critical log entries from telegramPoller",
      "conditionMatchedLog": {
        "filter": "resource.type=\"cloud_function\" AND resource.labels.function_name=\"telegramPoller\" AND severity>=CRITICAL"
      }
    }
  ],
  "alertStrategy": {
    "notificationRateLimit": {
      "period": "300s"
    },
    "autoClose": "604800s"
  },
  "enabled": true
}
